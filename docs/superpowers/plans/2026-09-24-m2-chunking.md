# M2 切片策略 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现三种切片策略（`fixed` / `semantic` / `structural`），使它们共享同一 `target_size`、差异只在"切在哪里"，并产出可核验的公平性报告。

**Architecture:** 先修 converter 的 MDX 归一化（不修则 8.5% 语料的 `structural` 结果是垃圾），再实现 `chunking/` 四个模块：`base.py` 定义 `Chunker` 协议、`SizeStats` 与共享的段落累积器；三个策略各自实现 `chunk(doc) -> list[Chunk]`。公平性检查是**一个真的会跑的产物**（`scripts/check_fairness.py`），不是测试里的断言。

**Tech Stack:** Python 3.11+ · uv · markdown-it-py · Pydantic + Pydantic Settings · PyYAML · pytest

**设计文档：** `docs/superpowers/specs/2026-09-24-m2-chunking-design.md`（M2 权威来源）；`docs/superpowers/specs/2026-09-18-docsentry-design.md`（主设计文档，§6.2.1 / §6.3 已按本计划修正）

---

## 开工前已实测的事实（2026-09-24，不必重查）

| 事实 | 值 |
|---|---|
| 语料 | 774 篇 / 14,536,287 字符（`data/corpus.jsonl`） |
| MDX 容器让正文被读成代码块 | **1,235,578 字符 = 8.5%**，211 篇 |
| 同上，归一化后 | **0 字符**（774 篇中 677 篇被改动） |
| 标题总数 归一化前 → 后 | 9,007 → **11,565** |
| `build-client.md` 标题数 | 2 → **127**（假标题 **0**） |
| 非叶 section 自己的正文 | **2,618,528 字符 = 18.0%**（字面实现会全丢） |
| 最大单 chunk（字面原子规则） | **106,728 字符**（LangChain 的 "All tools and toolkits" 表格） |
| 平均 chunk 尺寸（MDX 修复前） | `fixed` 792 · `structural` 857（比值 1.08×） |

⚠️ **上表最后两行的尺寸数字是在 MDX 修复前测的**，Task 14 会重测并更新设计文档与 README。

**验证过的关键代码**（在 scratch 里跑通才写进本计划，不是草稿）：

| 代码 | 验证方式 | 结果 |
|---|---|---|
| Task 1–3 `normalise()` | `build-client.md` + 全语料 | 2 → **127** 标题、假标题 **0**、语料级 1,235,578 → **0** 字符 |
| Task 8 `_sections()` | 5 个用例 + 全语料覆盖率 | 5/5 通过；覆盖率 **98.4%**（leaf-only 78.8%） |

> ⚠️ **这段"已验证"有边界。** 执行 Task 1 时，实现 subagent 在**已标为验证过的** `scan()` 里又找出一个缺陷：
> `min_indent` 只统计 `kind == "content"` 的行，于是直接子节点全是标签的容器（`<Tabs>` 下只有 `<Tab>`）
> 会取到**后代**的缩进值。我的验证只跑了 `build-client.md` 那一条路径——那里恰好不触发。**单点验证不等于性质验证。**
> 语料级实测：2362 个 frame 中 318 个取错（几乎全是 Tabs / Steps / AccordionGroup）。已修正。

Task 8 的实现在验证中**改了两版**，两处都是写计划的初稿里真实存在的缺陷：

1. **正文顺序反了** —— 初稿在 section 被弹出时才发正文，导致输出是 C、B、A 而不是 A、B、C。
   改为"子标题一到就发父 section 的正文"。
2. **标题行混进正文且重复** —— `heading_open` 与随后的 `inline` 共享同一个 token map，
   `paragraph_open`/`inline`/`paragraph_close` 亦然，逐 token 累加会把每行追加多次、
   并把标题行本身算进正文。改为**token 流只定位标题、正文按行号切片**。

两条对应的测试已在 Task 8 的 Step 1 里（`test_document_order_is_preserved`、
`test_heading_text_is_not_part_of_the_body`）。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `src/docsentry/corpus/mdx.py` | **新增** — MDX 容器归一化（纯函数，不依赖任何项目模块） |
| `src/docsentry/corpus/converters/markdown.py` | **修改** — 在剥样板块之后接上 MDX 归一化 |
| `src/docsentry/models.py` | **修改** — 新增 `Chunk` |
| `src/docsentry/config.py` | **修改** — 新增 `ChunkingConfig` + 加载函数 |
| `src/docsentry/corpus/loader.py` | **新增** — `load_corpus(path)` |
| `src/docsentry/chunking/base.py` | **新增** — `Chunker` 协议、`SizeStats`、共享段落累积器 |
| `src/docsentry/chunking/fixed.py` | **新增** — `FixedSizeChunker` |
| `src/docsentry/chunking/semantic.py` | **新增** — `SemanticChunker` |
| `src/docsentry/chunking/structural.py` | **新增** — `StructuralChunker`（核心） |
| `src/docsentry/chunking/registry.py` | **新增** — 策略名 → 实例 |
| `configs/chunking.yaml` | **新增** — 切片参数 |
| `scripts/check_fairness.py` | **新增** — 公平性检查产物 |
| `pyproject.toml` | **修改** — 显式声明 `markdown-it-py` |

**分层依赖方向**（单向，不得反向）：

```
scripts/  →  chunking/registry.py  →  {fixed,semantic,structural}.py  →  chunking/base.py
                                                                       →  models.py
          →  corpus/loader.py      →  models.py
corpus/converters/markdown.py  →  corpus/mdx.py        （不依赖 chunking）
```

`corpus/mdx.py` **不 import 任何项目模块** —— 它是纯字符串变换，这样可以独立测试。

---

## Task 1: `corpus/mdx.py` — 容器扫描

**Files:**
- Create: `src/docsentry/corpus/mdx.py`
- Test: `tests/test_mdx.py`

这一步只做扫描：找出容器标签、给每行标注归属、计算每个容器的最小内容缩进。**不产出任何文本**，所以测试全是结构断言。

- [ ] **Step 1: 写失败的测试**

```python
# tests/test_mdx.py
"""MDX container normalisation -- design spec 6.2.1.

The corpus is Mintlify-flavoured MDX: tabbed content is indented inside
<Tabs>/<Tab>/<CodeGroup>, and CommonMark reads indented content as a *code
block*. Measured on the real corpus: 1,235,578 characters (8.5%) arrive
misparsed, and `build-client.md` shows the parser only 2 of its 127 headings.
"""

from docsentry.corpus.mdx import scan


def test_scan_finds_nested_container_indents():
    text = (
        "<Tabs>\n"
        '  <Tab title="Python">\n'
        "    ## Requirements\n"
        "\n"
        "    <CodeGroup>\n"
        "      ```bash\n"
        "      uv venv\n"
        "      ```\n"
        "    </CodeGroup>\n"
        "  </Tab>\n"
        "</Tabs>\n"
    )
    frames, owner, kind = scan(text.splitlines(keepends=True))

    layout = [f for f in frames if f.tag in ("Tabs", "Tab", "CodeGroup")]
    assert [f.tag for f in layout] == ["Tabs", "Tab", "CodeGroup"]
    # each level adds 2: <Tabs> content at 2, <Tab> content at 4, <CodeGroup> at 6
    assert [f.min_indent for f in layout] == [2, 4, 6]
    assert layout[1].title == "Python"
    assert layout[0].title is None


def test_scan_ignores_tag_lookalikes_inside_fences():
    text = (
        "```html\n"
        "<Tabs>\n"
        "</Tabs>\n"
        "```\n"
    )
    frames, _owner, _kind = scan(text.splitlines(keepends=True))
    assert frames == []


def test_scan_assigns_each_content_line_to_its_innermost_container():
    text = (
        "<Tabs>\n"
        '  <Tab title="Python">\n'
        "    ## Requirements\n"
        "  </Tab>\n"
        "</Tabs>\n"
        "outside\n"
    )
    _frames, owner, kind = scan(text.splitlines(keepends=True))
    lines = text.splitlines(keepends=True)
    idx = {line.strip(): i for i, line in enumerate(lines)}

    assert owner[idx["## Requirements"]].tag == "Tab"
    assert owner[idx["outside"]] is None
    assert kind[idx["## Requirements"]] == "content"
    assert kind[idx["<Tabs>"]] == "open"
    assert kind[idx["</Tabs>"]] == "close"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_mdx.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'docsentry.corpus.mdx'`

- [ ] **Step 3: 声明依赖**

`markdown-it-py` 当前是**传递依赖**（能 import 但没写进 `pyproject.toml`）。本计划要直接 import 它，必须显式声明——否则哪天上游换依赖就会断。在 `dependencies` 里加一行：

```toml
    "markdown-it-py>=3.0",
```

然后：

```bash
uv sync
git diff uv.lock    # 期望：uv.lock 有变更（CLAUDE.md 约定 #7：改依赖必须一起提交）
```

- [ ] **Step 4: 写实现**

```python
# src/docsentry/corpus/mdx.py
"""MDX layout-container normalisation -- design spec 6.2.1.

MCP and LangChain pages are Mintlify-flavoured MDX: tabbed content lives inside
``<Tabs>`` / ``<Tab title="X">`` / ``<CodeGroup>`` and is indented 4-6 spaces.
CommonMark reads indented content as a **code block**, so on the real corpus
1,235,578 characters (8.5%, 211 documents) reach the parser as "code" -- every
heading inside them is invisible, ``kind`` would be judged ``"code"``, and the
chunker's atomic-unit rule would preserve the whole region verbatim.

This module is a pure string transform with no project imports, so it can be
tested without a corpus, a network, or a chunker.
"""

from __future__ import annotations

import re
from dataclasses import dataclass, field

FENCE_RE = re.compile(r"^[ \t]*(```|~~~)")
OPEN_RE = re.compile(
    r"^[ \t]*<(?P<tag>[A-Z][A-Za-z0-9]*)"
    r"(?:\s+title=(?:\"(?P<dq>[^\"]*)\"|'(?P<sq>[^']*)'))?"
    r"[^>]*>\s*$"
)
CLOSE_RE = re.compile(r"^[ \t]*</(?P<tag>[A-Z][A-Za-z0-9]*)>\s*$")
HEADING_RE = re.compile(r"^(#{1,6})\s+(.*)")
TAG_ANY_RE = re.compile(r"^[ \t]*</?[A-Z][A-Za-z0-9]*[^>]*>\s*$")

# Layout containers: their content is real prose/code that must be surfaced.
LAYOUT = frozenset({"Tabs", "Tab", "CodeGroup", "Accordion", "AccordionGroup", "Steps", "Step"})
# Semantic tags: the tag itself carries no structure we preserve -- drop the
# tag, keep the text it wraps.
SEMANTIC = frozenset({"Note", "Warning", "Tip", "Info", "Check", "Card", "CardGroup",
                      "Columns", "Column"})


@dataclass
class Frame:
    """One open layout container."""

    tag: str
    title: str | None
    open_i: int
    close_i: int = -1
    min_indent: int = 0
    titled_depth: int = 0   # titled containers from the outermost ancestor to here
    level: int = 0          # synthetic heading level; 0 means "emit no heading"

    @property
    def index(self) -> int:
        return self.open_i


def indent_of(line: str) -> int:
    return len(line) - len(line.lstrip(" \t"))


def scan(lines: list[str]) -> tuple[list[Frame], list[Frame | None], list[str]]:
    """One pass over ``lines``.

    Returns ``(frames, owner, kind)``:

    * ``frames`` -- every layout container, in nesting order, with ``min_indent``
      filled in (the smallest indent among its own content lines).
    * ``owner``  -- per line, the innermost layout container it belongs to.
    * ``kind``   -- per line, ``"open"`` / ``"close"`` / ``"content"``.
    """
    frames: list[Frame] = []
    stack: list[Frame] = []
    owner: list[Frame | None] = []
    kind: list[str] = []
    in_fence = False

    for i, line in enumerate(lines):
        is_tag = bool(TAG_ANY_RE.match(line))
        marker = bool(FENCE_RE.match(line))

        # Container tags are only recognised outside fences -- a fenced HTML
        # sample must not be mistaken for a container.
        if not in_fence and not marker and is_tag:
            mc = CLOSE_RE.match(line)
            if mc:
                for k in range(len(stack) - 1, -1, -1):
                    if stack[k].tag == mc.group("tag"):
                        stack[k].close_i = i
                        del stack[k:]
                        break
                kind.append("close")
                owner.append(None)
                continue

            mo = OPEN_RE.match(line)
            if mo and mo.group("tag") in LAYOUT:
                title = mo.group("dq") or mo.group("sq")
                frames.append(Frame(
                    tag=mo.group("tag"),
                    title=title,
                    open_i=i,
                    titled_depth=sum(1 for f in stack if f.title) + (1 if title else 0),
                ))
                stack.append(frames[-1])
                kind.append("open")
                owner.append(None)
                continue
            if mo and mo.group("tag") in SEMANTIC:
                kind.append("close")   # dropped either way
                owner.append(None)
                continue

        if marker:
            in_fence = not in_fence

        kind.append("content")
        owner.append(stack[-1] if stack else None)

    for frame in frames:
        if frame.close_i < 0:
            frame.close_i = len(lines)
        # Every non-blank line inside the container, tag lines included. Filtering
        # to kind == "content" would skip a container's own child *tags*, so a
        # <Tabs> whose only direct children are <Tab> tags would report the
        # descendants' indent (4) instead of its own content level (2) -- and
        # subtracting 4 from a 2-space-indented line eats two columns of text.
        # Measured corpus-wide: 318 of 2362 frames, almost all Tabs / Steps /
        # AccordionGroup, i.e. exactly the containers whose children are tags.
        mins = [indent_of(lines[i])
                for i in range(frame.open_i + 1, frame.close_i)
                if lines[i].strip() and kind[i] in ("content", "open", "close")]
        frame.min_indent = min(mins) if mins else 0

    return frames, owner, kind
```

- [ ] **Step 5: 跑测试确认通过**

Run: `uv run pytest tests/test_mdx.py -v`
Expected: 3 passed

- [ ] **Step 6: 提交**

```bash
git add src/docsentry/corpus/mdx.py tests/test_mdx.py pyproject.toml uv.lock
git commit -m "feat(m2): MDX 容器扫描 — 定位被读成代码块的正文"
```

---

## Task 2: `corpus/mdx.py` — 归一化输出

**Files:**
- Modify: `src/docsentry/corpus/mdx.py`
- Test: `tests/test_mdx.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/test_mdx.py`：

```python
from docsentry.corpus.mdx import normalise


def test_normalise_removes_tags_and_dedents_content():
    text = (
        "<Tabs>\n"
        '  <Tab title="Python">\n'
        "    ## Requirements\n"
        "\n"
        "    Needs Python 3.10.\n"
        "  </Tab>\n"
        "</Tabs>\n"
    )
    out = normalise(text)
    assert "<Tabs>" not in out and "<Tab" not in out
    assert "## Requirements" in out
    assert "\n    Needs Python 3.10." not in out   # dedented to column 0


def test_normalise_keeps_fenced_code_fenced():
    """The trap: a fence inside a container is indented too. If only the
    surrounding prose is dedented the fence stays indented, or if the tags are
    left in place they open an HTML block that swallows it -- either way a
    shell comment like `# Create virtual environment` is promoted to an H1."""
    text = (
        "<Tabs>\n"
        '  <Tab title="Python">\n'
        "    <CodeGroup>\n"
        "      ```bash\n"
        "      # Create virtual environment\n"
        "      uv venv\n"
        "      ```\n"
        "    </CodeGroup>\n"
        "  </Tab>\n"
        "</Tabs>\n"
    )
    out = normalise(text)

    from markdown_it import MarkdownIt

    tokens = MarkdownIt("commonmark").parse(out)
    headings = [tokens[k + 1].content
                for k, t in enumerate(tokens) if t.type == "heading_open"]
    assert "Create virtual environment" not in headings
    assert any(t.type == "fence" for t in tokens)
    assert "uv venv" in next(t.content for t in tokens if t.type == "fence")


def test_normalise_drops_semantic_wrappers_but_keeps_text():
    text = "<Note>\n  Check your API key.\n</Note>\n"
    out = normalise(text)
    assert "<Note>" not in out
    assert "Check your API key." in out
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_mdx.py -v -k normalise`
Expected: FAIL — `ImportError: cannot import name 'normalise'`

- [ ] **Step 3: 写实现**

追加到 `src/docsentry/corpus/mdx.py`：

```python
def normalise(text: str) -> str:
    """Flatten MDX layout containers into plain Markdown.

    Three rules, each load-bearing:

    1. **Container tags are removed.** Left in place they open a CommonMark
       HTML block (type 6) that runs to the next blank line, swallowing the
       fence right after ``<CodeGroup>`` -- which is how shell comments inside
       code ended up promoted to H1s.
    2. **Content is dedented by its innermost container's ``min_indent``**
       (absolute, not summed: a line at indent 6 inside containers whose minima
       are 2/4/6 would lose 12 columns if the minima were added).
    3. **Titled containers become synthetic headings**, one level below the
       heading their outermost ancestor hangs under. Without this the seven
       language tabs of ``build-client.md`` produce seven identical subtrees
       competing for the same top-5 slots.
    """
    lines = text.splitlines(keepends=True)
    frames, owner, kind = scan(lines)
    _resolve_levels(lines, frames, kind)

    out: list[str] = []
    for i, line in enumerate(lines):
        if kind[i] != "content":
            frame = next((f for f in frames if f.open_i == i), None)
            if frame is not None and frame.level:
                out.append(f"{'#' * frame.level} {frame.title}\n")
            continue
        if not line.strip():
            out.append(line)
            continue

        frame = owner[i]
        dedent = frame.min_indent if frame else 0
        body = line[dedent:] if len(line) > dedent else line.lstrip(" \t")

        match = HEADING_RE.match(body)
        if match and not _inside_fence(lines, kind, i):
            shift = frame.titled_depth if frame else 0
            if shift:
                body = f"{'#' * min(len(match.group(1)) + shift, 6)} {match.group(2)}"
        out.append(body)

    return "".join(out)


def _resolve_levels(lines: list[str], frames: list[Frame], kind: list[str]) -> None:
    """Give every titled container a heading level.

    Computed statically, not from emit-time state: the previous tab's headings
    leak into the running level and each successive tab comes out one deeper
    (observed h2, h3, h4, h5, h6, h6, h6 before this was made static).
    """
    outside = 0
    level_before: list[int] = []
    in_fence = False
    for i, line in enumerate(lines):
        level_before.append(outside)
        if kind[i] == "content" and FENCE_RE.match(line):
            in_fence = not in_fence
        if kind[i] == "content" and not in_fence:
            match = HEADING_RE.match(line)
            if match and not owner_depth(frames, i):
                outside = len(match.group(1))

    for frame in frames:
        if not frame.title:
            frame.level = 0
            continue
        ancestors = [f for f in frames if f.open_i < frame.open_i < f.close_i]
        base = max((a.level for a in ancestors if a.level), default=level_before[frame.open_i])
        frame.level = min((base or 0) + frame.titled_depth, 6) or min(frame.titled_depth, 6)


def owner_depth(frames: list[Frame], i: int) -> int:
    return sum(1 for f in frames if f.open_i < i < f.close_i)


def _inside_fence(lines: list[str], kind: list[str], upto: int) -> bool:
    state = False
    for j in range(upto):
        if kind[j] == "content" and FENCE_RE.match(lines[j]):
            state = not state
    return state
```

> ⚠️ **注意 `_resolve_levels` 里的两处细节**，它们各自对应一次真实的踩坑：
> - `level_before` 必须在**循环开头**记录，即在处理该行之前——否则容器自身那一行会把
>   `outside` 更新成容器内部的标题层级。
> - 层级必须**按嵌套顺序解析**（`frames` 已经是开放顺序），且祖先的 `level` 可能还是 0
>   （无 title 的容器不产出标题）。`base or 0` 与末尾的 `or min(...)` 处理这个情况。
>
> 这两条是本计划里已经验证过的写法；**改这两行前先跑 Task 2 的第三个测试**。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_mdx.py -v`
Expected: 6 passed

- [ ] **Step 5: 提交**

```bash
git add src/docsentry/corpus/mdx.py tests/test_mdx.py
git commit -m "feat(m2): MDX 归一化 — 摘标签 + 按嵌套 dedent + 合成标题"
```

---

## Task 3: 接进 `MarkdownConverter`

**Files:**
- Modify: `src/docsentry/corpus/converters/markdown.py`
- Test: `tests/test_converters.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/test_converters.py`：

```python
def test_converter_expands_mdx_containers(tmp_path):
    """Design spec 6.2.1: normalisation happens at convert time, before chunking
    ever sees the document."""
    from docsentry.corpus.converters.markdown import MarkdownConverter

    path = tmp_path / "page.md"
    path.write_bytes(
        b"# Guide\n"
        b"<Tabs>\n"
        b'  <Tab title="Python">\n'
        b"    ## Setup\n"
        b"  </Tab>\n"
        b"</Tabs>\n"
    )
    converted = MarkdownConverter().convert(path)

    assert "<Tabs>" not in converted.text
    assert "## Setup" in converted.text or "### Setup" in converted.text
    assert converted.title == "Guide"


def test_converter_still_strips_the_index_banner(tmp_path):
    """The existing normalisation must survive the new one."""
    from docsentry.corpus.converters.markdown import MarkdownConverter

    path = tmp_path / "page.md"
    path.write_bytes(
        b"> ## Documentation Index\n"
        b"> - [A](a.md)\n"
        b"\n"
        b"# Real Title\n"
        b"\n"
        b"Body.\n"
    )
    converted = MarkdownConverter().convert(path)

    assert "Documentation Index" not in converted.text
    assert converted.title == "Real Title"
```

> 夹具一律用 `write_bytes` 而非 `write_text`：Windows 下 `write_text` 是文本模式写入
> （`newline=None`），`\n` 会落盘成 `\r\n`；任何断言 bytes 的测试都会因此不可满足。

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_converters.py -v -k mdx`
Expected: FAIL — `<Tabs>` still present in `converted.text`

- [ ] **Step 3: 写实现**

在 `markdown.py` 的 import 区加：

```python
from docsentry.corpus.mdx import normalise
```

并把 `convert` 改成：

```python
    def convert(self, path: Path) -> ConvertedDoc:
        raw = path.read_text(encoding="utf-8", errors="replace")
        text = normalise(strip_index_banner(raw))
        return ConvertedDoc(text=text, title=first_heading(text), origin_format="md")
```

**顺序不可颠倒**：先剥样板块（它在任何标题之前），再展开 MDX 容器。

把模块 docstring 的"两处归一化"改成"三处"，并补一段说明 MDX 归一化的理由（引用实测数字：1,235,578 字符 = 8.5%、211 篇）。

- [ ] **Step 4: 跑全量测试**

Run: `uv run pytest -q`
Expected: 全绿（M1 的 192 条 + 新增的 8 条）。**若有 M1 测试挂了，先查是不是它自己的夹具含 MDX 标签**，不要直接改断言。

- [ ] **Step 5: 重建语料**

```bash
uv run scripts/fetch_corpus.py --update
```

Expected：约 4–5 分钟（约 2,370 次请求）。完成后：

```bash
uv run scripts/health.py
```

Expected：`documents: 774`（篇数不变——归一化改内容不改篇数）。

- [ ] **Step 6: 提交**

```bash
git add src/docsentry/corpus/converters/markdown.py tests/test_converters.py data/ 2>/dev/null
git commit -m "fix(corpus): converter 展开 MDX 容器 — 8.5% 语料不再被读成代码块"
```

> `data/` 整体 gitignore，本步骤实际只提交源码与测试。

---

## Task 4: `Chunk` 模型 + 切片配置 + 语料加载器

**Files:**
- Modify: `src/docsentry/models.py`
- Modify: `src/docsentry/config.py`
- Create: `src/docsentry/corpus/loader.py`
- Create: `configs/chunking.yaml`
- Test: `tests/test_models.py`, `tests/test_config.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/test_models.py`：

```python
def test_chunk_id_is_stable_and_strategy_scoped():
    from docsentry.models import make_chunk_id

    a = make_chunk_id("doc123", 0, "structural")
    assert a == make_chunk_id("doc123", 0, "structural")
    # the same position under a different strategy is a different chunk
    assert a != make_chunk_id("doc123", 0, "fixed")
    assert a != make_chunk_id("doc123", 1, "structural")
    assert len(a) == 16
```

追加到 `tests/test_config.py`：

```python
def test_chunking_config_defaults(tmp_path):
    from docsentry.config import load_chunking_config

    path = tmp_path / "chunking.yaml"
    path.write_text("target_size: 1000\n", encoding="utf-8")
    cfg = load_chunking_config(path)

    assert cfg.target_size == 1000
    assert cfg.overlap == 200
    assert cfg.max_atomic_size == 4000
    assert cfg.max_code_size == 3000


def test_chunking_config_rejects_overlap_ge_target(tmp_path):
    """overlap >= target_size makes the fixed window never advance."""
    import pytest

    from docsentry.config import load_chunking_config

    path = tmp_path / "chunking.yaml"
    path.write_text("target_size: 500\noverlap: 500\n", encoding="utf-8")
    with pytest.raises(ValueError):
        load_chunking_config(path)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_models.py tests/test_config.py -v -k "chunk"`
Expected: FAIL — `ImportError: cannot import name 'make_chunk_id'`

- [ ] **Step 3: 写 `models.py`**

`models.py` 的 docstring 已声明 M1 只放语料层模型、`Chunk` 归 M2。把 docstring 改成"``Chunk`` 由 M2 定义"，并追加：

```python
def make_chunk_id(doc_id: str, ordinal: int, strategy: str) -> str:
    """Stable 16-hex identity for one chunk -- sha256(doc_id + ordinal + strategy)[:16].

    Strategy is part of the identity on purpose: the same span of text under
    ``fixed`` and under ``structural`` are different chunks, and the vector
    store keys collections by strategy.
    """
    return hashlib.sha256(f"{doc_id}{ordinal}{strategy}".encode("utf-8")).hexdigest()[:16]


@dataclass
class Chunk:
    """One retrievable unit -- design spec 5.

    ``indexed_at`` is ``None`` at chunking stage; ``build_index`` (M3) stamps it
    when the chunk is actually upserted. Same convention as ``Document``: the
    authoritative value lives in the Qdrant payload, and guessing it earlier
    would misreport provenance by however long the corpus sat on disk.

    ``split_atomic`` records that the chunk is a piece of an atomic unit
    (code block or table) that exceeded ``max_atomic_size`` and had to be cut.
    The evaluation reports on these chunks separately -- "we never cut a code
    block" is a claim the system cannot honestly make, so it records when it
    did instead (design spec 6.9).
    """

    chunk_id: str
    doc_id: str
    text: str
    breadcrumb: list[str]
    source: str
    version: str
    url: str
    kind: str                    # "prose" | "code" | "table" | "mixed"
    strategy: str
    char_count: int
    split_atomic: bool = False
    indexed_at: datetime | None = None
```

- [ ] **Step 4: 写 `config.py`**

在 `config.py` 追加：

```python
class ChunkingConfig(BaseModel):
    """Chunking parameters -- design spec 6.3.

    ``target_size`` is shared by all three strategies; that is the fairness
    constraint, not a convenience. ``overlap`` applies to ``fixed`` alone:
    overlap compensates for a hard cut not knowing where a boundary is, and the
    two structure-aware strategies cut *at* boundaries, so they do not need it.
    Their indexed-character total is therefore 1.00x the corpus against
    ``fixed``'s 1.25x -- disclosed in the report rather than equalised.
    """

    target_size: int = 1000
    overlap: int = 200
    max_atomic_size: int = 4000
    max_code_size: int = 3000

    @model_validator(mode="after")
    def _overlap_must_advance(self):
        if self.overlap >= self.target_size:
            raise ValueError("overlap must be smaller than target_size, or the fixed window never advances")
        return self


def load_chunking_config(path: Path) -> ChunkingConfig:
    if not path.exists():
        raise FileNotFoundError(f"chunking config not found: {path}")
    raw = yaml.safe_load(path.read_text(encoding="utf-8")) or {}
    return ChunkingConfig.model_validate(raw)
```

并在 `Settings` 里加一行 `chunking_config: Path = Path("configs/chunking.yaml")`。

- [ ] **Step 5: 写 `configs/chunking.yaml`**

```yaml
# Chunking parameters -- design spec 6.3
#
# target_size is shared by all three strategies. That is the fairness
# constraint: if the sizes differ, an accuracy gap cannot be attributed to
# *where* the split happens rather than *how big* the chunks are.
target_size: 1000

# Applies to `fixed` only. Overlap compensates for a hard cut being blind to
# boundaries; `semantic` and `structural` cut at boundaries and do not need it.
overlap: 200

# Code blocks and tables are never cut -- unless they exceed this, in which
# case cutting is mandatory (a 106,728-character table was measured on the real
# corpus, and the embedding model truncates it silently).
max_atomic_size: 4000

# Above this, an oversized code block is split at blank lines and top-level
# def/class/function boundaries instead of at arbitrary characters.
max_code_size: 3000
```

- [ ] **Step 6: 写 `corpus/loader.py`**

```python
# src/docsentry/corpus/loader.py
"""Read ``data/corpus.jsonl`` back into ``Document`` objects."""

from __future__ import annotations

import json
from collections.abc import Iterator
from pathlib import Path

from docsentry.models import Document


def load_corpus(path: Path) -> Iterator[Document]:
    """Yield every document in a corpus file.

    Deliberately a generator: the real corpus is ~15 MB and chunking streams it,
    so materialising a list would double peak memory for no gain.
    """
    with path.open(encoding="utf-8") as handle:
        for line in handle:
            line = line.strip()
            if line:
                yield Document.from_json(json.loads(line))
```

- [ ] **Step 7: 跑测试**

Run: `uv run pytest tests/test_models.py tests/test_config.py -v -k "chunk or json"`
Expected: 全绿

- [ ] **Step 8: 提交**

```bash
git add src/docsentry/models.py src/docsentry/config.py src/docsentry/corpus/loader.py configs/chunking.yaml tests/
git commit -m "feat(m2): Chunk 模型 + 切片配置 + 语料加载器"
```

---

## Task 5: `chunking/base.py` — 协议、统计、共享累积器

**Files:**
- Create: `src/docsentry/chunking/__init__.py`
- Create: `src/docsentry/chunking/base.py`
- Test: `tests/test_chunking_base.py`

- [ ] **Step 1: 写失败的测试**

```python
# tests/test_chunking_base.py
from docsentry.chunking.base import SizeStats, accumulate_paragraphs


def test_accumulate_respects_the_target():
    text = "a" * 400 + "\n\n" + "b" * 400 + "\n\n" + "c" * 400
    pieces = accumulate_paragraphs(text, target=1000)

    assert len(pieces) == 2
    assert pieces[0].startswith("a")
    assert pieces[1].startswith("c")


def test_accumulate_never_splits_a_paragraph():
    """A paragraph longer than the target is emitted whole -- it is one unit."""
    text = "x" * 5000
    assert accumulate_paragraphs(text, target=1000) == [text]


def test_accumulate_keeps_short_text_whole():
    assert accumulate_paragraphs("short", target=1000) == ["short"]


def test_size_stats_reports_what_the_fairness_check_needs():
    stats = SizeStats.from_sizes([100, 200, 300])
    assert stats.count == 3
    assert stats.mean == 200
    assert stats.median == 200
    assert stats.total_chars == 600
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_chunking_base.py -v`
Expected: FAIL — no module named `docsentry.chunking`

- [ ] **Step 3: 写实现**

`src/docsentry/chunking/__init__.py` 留空。

```python
# src/docsentry/chunking/base.py
"""The ``Chunker`` abstraction -- design spec 4.3 and 6.3.

Every strategy takes a ``Document`` and returns ``list[Chunk]``. They share one
``target_size`` and one notion of "how big is a chunk", because the whole point
of the M2 comparison is that the only difference between them is *where* they
cut -- see ``SizeStats``.
"""

from __future__ import annotations

import re
from dataclasses import dataclass
from typing import Protocol, runtime_checkable

from docsentry.config import ChunkingConfig
from docsentry.models import Chunk, Document


def accumulate_paragraphs(text: str, target: int) -> list[str]:
    """Group blank-line-separated paragraphs up to ``target`` characters.

    A paragraph longer than ``target`` is emitted whole: it is one unit, and
    cutting it is exactly what ``fixed`` is here to demonstrate the cost of.
    """
    if len(text) <= target:
        return [text]
    pieces: list[str] = []
    buffer = ""
    for paragraph in text.split("\n\n"):
        if buffer and len(buffer) + len(paragraph) + 2 > target:
            pieces.append(buffer)
            buffer = paragraph
        else:
            buffer = f"{buffer}\n\n{paragraph}" if buffer else paragraph
    if buffer:
        pieces.append(buffer)
    return pieces


@dataclass
class SizeStats:
    """What the fairness report publishes -- design spec 6.3.

    ``total_chars`` is here because it is the number that exposes the overlap
    difference: ``fixed`` indexes 1.25x the corpus, the structure-aware
    strategies 1.00x. Mean size alone would hide it.
    """

    count: int
    mean: float
    median: float
    total_chars: int

    @classmethod
    def from_sizes(cls, sizes: list[int]) -> SizeStats:
        from statistics import mean, median

        if not sizes:
            return cls(count=0, mean=0.0, median=0.0, total_chars=0)
        return cls(count=len(sizes), mean=mean(sizes), median=median(sizes),
                   total_chars=sum(sizes))


@runtime_checkable
class Chunker(Protocol):
    name: str

    def chunk(self, doc: Document) -> list[Chunk]:
        """Split one document. ``Document.indexed_at`` is ``None`` at this stage."""
        ...
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_chunking_base.py -v`
Expected: 4 passed

- [ ] **Step 5: 提交**

```bash
git add src/docsentry/chunking/ tests/test_chunking_base.py
git commit -m "feat(m2): Chunker 协议 + SizeStats + 共享段落累积器"
```

---

## Task 6: `FixedSizeChunker`

**Files:**
- Create: `src/docsentry/chunking/fixed.py`
- Test: `tests/test_chunking_fixed.py`

- [ ] **Step 1: 写失败的测试**

```python
# tests/test_chunking_fixed.py
from datetime import datetime, timezone

import pytest

from docsentry.chunking.fixed import FixedSizeChunker
from docsentry.config import ChunkingConfig
from docsentry.models import Document, SourceKind


@pytest.fixture
def doc():
    return Document(
        doc_id="doc1", source="mcp", kind=SourceKind.LLMS_TXT, version="2026-07-28",
        url="https://example.com/a.md", title="A", path="a.md",
        content="x" * 2500, content_hash="h", origin_format="md",
        fetched_at=datetime(2026, 9, 24, tzinfo=timezone.utc),
    )


def test_fixed_window_advances_by_target_minus_overlap(doc):
    chunker = FixedSizeChunker(ChunkingConfig(target_size=1000, overlap=200))
    chunks = chunker.chunk(doc)

    # 2500 chars, window 1000, stride 800 -> starts at 0, 800, 1600, 2400
    assert len(chunks) == 4
    assert chunks[0].text == "x" * 1000
    assert chunks[-1].text == "x" * 100       # the tail


def test_fixed_chunks_carry_document_provenance(doc):
    chunker = FixedSizeChunker(ChunkingConfig())
    chunk = chunker.chunk(doc)[0]

    assert chunk.doc_id == "doc1"
    assert chunk.source == "mcp"
    assert chunk.version == "2026-07-28"
    assert chunk.url == "https://example.com/a.md"
    assert chunk.strategy == "fixed"
    assert chunk.breadcrumb == []
    assert chunk.char_count == len(chunk.text)
    assert chunk.indexed_at is None


def test_fixed_chunk_ids_are_unique_and_stable(doc):
    chunker = FixedSizeChunker(ChunkingConfig())
    first = chunker.chunk(doc)
    again = chunker.chunk(doc)

    assert [c.chunk_id for c in first] == [c.chunk_id for c in again]
    assert len({c.chunk_id for c in first}) == len(first)


def test_fixed_handles_document_shorter_than_one_window(doc):
    short = Document(**{**doc.__dict__, "content": "abc"})
    chunks = FixedSizeChunker(ChunkingConfig()).chunk(short)

    assert len(chunks) == 1
    assert chunks[0].text == "abc"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_chunking_fixed.py -v`
Expected: FAIL — `ModuleNotFoundError: docsentry.chunking.fixed`

- [ ] **Step 3: 写实现**

```python
# src/docsentry/chunking/fixed.py
"""``FixedSizeChunker`` -- the baseline -- design spec 6.3.1.

A hard character window with overlap. It makes no structural judgement at all,
which is the point: it exists to show what those judgements are worth.

The overlap is also why this strategy indexes 1.25x the corpus while the other
two index 1.00x. That difference is published, not equalised -- overlap is a
compensation for cutting blind, and the structure-aware strategies do not need
it (design spec 6.3).
"""

from __future__ import annotations

from docsentry.config import ChunkingConfig
from docsentry.models import Chunk, Document, make_chunk_id


class FixedSizeChunker:
    name = "fixed"

    def __init__(self, config: ChunkingConfig) -> None:
        self.config = config

    def chunk(self, doc: Document) -> list[Chunk]:
        size = self.config.target_size
        stride = size - self.config.overlap
        content = doc.content

        chunks: list[Chunk] = []
        start = 0
        ordinal = 0
        while start < len(content):
            text = content[start:start + size]
            chunks.append(self._make(doc, text, ordinal))
            ordinal += 1
            if start + size >= len(content):
                break
            start += stride
        return chunks

    def _make(self, doc: Document, text: str, ordinal: int) -> Chunk:
        return Chunk(
            chunk_id=make_chunk_id(doc.doc_id, ordinal, self.name),
            doc_id=doc.doc_id,
            text=text,
            breadcrumb=[],
            source=doc.source,
            version=doc.version,
            url=doc.url,
            kind="prose",
            strategy=self.name,
            char_count=len(text),
        )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_chunking_fixed.py -v`
Expected: 4 passed

- [ ] **Step 5: 提交**

```bash
git add src/docsentry/chunking/fixed.py tests/test_chunking_fixed.py
git commit -m "feat(m2): FixedSizeChunker 基线"
```

---

## Task 7: `SemanticChunker`

**Files:**
- Create: `src/docsentry/chunking/semantic.py`
- Test: `tests/test_chunking_semantic.py`

- [ ] **Step 1: 写失败的测试**

```python
# tests/test_chunking_semantic.py
import pytest

from docsentry.chunking.semantic import SemanticChunker
from docsentry.config import ChunkingConfig
from docsentry.models import Document, SourceKind
from datetime import datetime, timezone


@pytest.fixture
def doc():
    return Document(
        doc_id="d", source="mcp", kind=SourceKind.LLMS_TXT, version="v",
        url="u", title="t", path="p",
        content="a" * 400 + "\n\n" + "b" * 400 + "\n\n" + "c" * 400,
        content_hash="h", origin_format="md",
        fetched_at=datetime(2026, 9, 24, tzinfo=timezone.utc),
    )


def test_semantic_splits_at_paragraph_boundaries(doc):
    chunks = SemanticChunker(ChunkingConfig(target_size=1000)).chunk(doc)

    assert len(chunks) == 2
    assert chunks[0].text.endswith("b" * 400)
    assert chunks[1].text.startswith("c")


def test_semantic_never_cuts_a_paragraph():
    """Each chunk must be a whole number of paragraphs."""
    long_doc = Document(
        doc_id="d2", source="s", kind=SourceKind.LLMS_TXT, version="v",
        url="u", title="t", path="p",
        content="\n\n".join(["p" * 300] * 10),
        content_hash="h", origin_format="md",
        fetched_at=datetime(2026, 9, 24, tzinfo=timezone.utc),
    )
    for chunk in SemanticChunker(ChunkingConfig(target_size=1000)).chunk(long_doc):
        for paragraph in chunk.text.split("\n\n"):
            assert paragraph == "p" * 300


def test_semantic_has_no_overlap(doc):
    """Indexed characters equal the source length -- no duplication."""
    chunks = SemanticChunker(ChunkingConfig()).chunk(doc)
    assert sum(c.char_count for c in chunks) == len(doc.content)
    assert all(c.strategy == "semantic" for c in chunks)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_chunking_semantic.py -v`
Expected: FAIL — module missing

- [ ] **Step 3: 写实现**

```python
# src/docsentry/chunking/semantic.py
"""``SemanticChunker`` -- the middle baseline -- design spec 6.3.2.

Accumulates paragraphs up to ``target_size`` and cuts at blank lines. It never
splits a paragraph, but it still has no idea a heading hierarchy exists -- which
is exactly the gap ``structural`` is meant to close.

No overlap: it cuts at real boundaries, so there is no blind cut to compensate
for (design spec 6.3).
"""

from __future__ import annotations

from docsentry.chunking.base import accumulate_paragraphs
from docsentry.config import ChunkingConfig
from docsentry.models import Chunk, Document, make_chunk_id


class SemanticChunker:
    name = "semantic"

    def __init__(self, config: ChunkingConfig) -> None:
        self.config = config

    def chunk(self, doc: Document) -> list[Chunk]:
        pieces = accumulate_paragraphs(doc.content, self.config.target_size)
        return [
            Chunk(
                chunk_id=make_chunk_id(doc.doc_id, ordinal, self.name),
                doc_id=doc.doc_id,
                text=text,
                breadcrumb=[],
                source=doc.source,
                version=doc.version,
                url=doc.url,
                kind="prose",
                strategy=self.name,
                char_count=len(text),
            )
            for ordinal, text in enumerate(pieces)
        ]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_chunking_semantic.py -v`
Expected: 3 passed

- [ ] **Step 5: 提交**

```bash
git add src/docsentry/chunking/semantic.py tests/test_chunking_semantic.py
git commit -m "feat(m2): SemanticChunker 中间基线"
```

---

## Task 8: `StructuralChunker` — 标题栈 + flush-on-close

**这是 M2 的核心。** 设计文档 §6.3.3 step 3/4 字面实现会丢掉 18.0% 的语料正文，本任务实现修正后的 `flush-on-close`。

**Files:**
- Create: `src/docsentry/chunking/structural.py`
- Test: `tests/test_chunking_structural.py`

- [ ] **Step 1: 写失败的测试**

```python
# tests/test_chunking_structural.py
from datetime import datetime, timezone

import pytest

from docsentry.chunking.structural import StructuralChunker
from docsentry.config import ChunkingConfig
from docsentry.models import Document, SourceKind


def make_doc(content: str) -> Document:
    return Document(
        doc_id="d", source="mcp", kind=SourceKind.LLMS_TXT, version="2026-07-28",
        url="https://example.com/a.md", title="A", path="a.md",
        content=content, content_hash="h", origin_format="md",
        fetched_at=datetime(2026, 9, 24, tzinfo=timezone.utc),
    )


@pytest.fixture
def chunker():
    return StructuralChunker(ChunkingConfig(target_size=1000))


def test_breadcrumb_reflects_the_heading_stack(chunker):
    doc = make_doc("# Guide\n\n## Setup\n\n### Prereqs\n\nInstall it.\n")
    chunks = chunker.chunk(doc)

    assert len(chunks) == 1
    assert chunks[0].breadcrumb == ["Guide", "Setup", "Prereqs"]
    assert "Install it." in chunks[0].text


def test_parent_section_body_is_not_dropped(chunker):
    """The 18.0% bug. Text under a heading that later gains a child belongs to
    that heading -- not to nothing. Measured on the real corpus, emitting only
    leaf sections dropped 2,618,528 characters."""
    doc = make_doc(
        "# Build a client\n"
        "\n"
        "This intro sits under the H1 but before the first H2.\n"
        "\n"
        "## Next steps\n"
        "\n"
        "Done.\n"
    )
    chunks = chunker.chunk(doc)

    texts = {c.chunk_id: c for c in chunks}
    assert any("This intro sits under the H1" in c.text for c in chunks), \
        "the parent section body vanished"
    assert any(c.breadcrumb == ["Build a client"] for c in chunks if "intro" in c.text)
    assert any(c.breadcrumb == ["Build a client", "Next steps"] for c in chunks)


def test_flush_happens_before_the_stack_pops(chunker):
    """Ordering matters: the accumulated text must be attributed to the section
    it was written under, not the one that closed it."""
    doc = make_doc("# A\n\nbody of A\n\n## B\n\nbody of B\n")
    chunks = {tuple(c.breadcrumb): c for c in chunker.chunk(doc)}

    assert "body of A" in chunks[("A",)].text
    assert "body of A" not in chunks[("A", "B")].text


def test_empty_sections_produce_no_chunk(chunker):
    doc = make_doc("# A\n\n## B\n\nreal content\n")
    chunks = chunker.chunk(doc)

    assert all(c.text.strip() for c in chunks)
    assert len(chunks) == 1


def test_document_order_is_preserved(chunker):
    """Emitting a section's body when it is *popped* reverses the document
    (C, B, A). Both a sibling and a child heading must flush the section above
    them the moment they arrive."""
    doc = make_doc("# A\n\nbody of A\n\n## B\n\nbody of B\n\n### C\n\nbody of C\n")
    chunks = chunker.chunk(doc)

    assert [c.breadcrumb for c in chunks] == [["A"], ["A", "B"], ["A", "B", "C"]]
    assert "body of A" in chunks[0].text
    assert "body of B" in chunks[1].text
    assert "body of C" in chunks[2].text


def test_heading_text_is_not_part_of_the_body(chunker):
    """`heading_open` and the `inline` after it share one token map, and
    `paragraph_open`/`inline`/`paragraph_close` share theirs -- accumulating
    bodies straight from token maps appends each line once per covering token
    and puts the heading line in the body."""
    doc = make_doc("# Guide\n\n## Setup\n\nReal content.\n")
    chunks = chunker.chunk(doc)

    for chunk in chunks:
        assert not chunk.text.lstrip().startswith("#"), chunk.text[:40]
    assert "Real content." in chunks[0].text
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_chunking_structural.py -v`
Expected: FAIL — module missing

- [ ] **Step 3: 写实现**

```python
# src/docsentry/chunking/structural.py
"""``StructuralChunker`` -- the core of M2 -- design spec 6.3.3.

Walks the ``markdown-it-py`` token stream, keeps a heading stack, and emits one
chunk per section body.

**flush-on-close, not "leaf sections".** The design doc originally said "每个叶子
section 合并为一个 chunk". Implemented literally that drops every non-leaf
section's own body: measured on the real corpus, 2,618,528 characters (18.0%).
The worst case is ``build-client.md``, whose 2,522 lines carry exactly two
headings -- the 80,448-character tutorial hangs under the H1, so the literal
reading emits only the 172 characters of "Next steps" and the page that teaches
MCP client building is unreachable. Text belongs to the section it is written
under, not to the section that happens to have no children.
"""

from __future__ import annotations

import re
from dataclasses import dataclass, field

from markdown_it import MarkdownIt

from docsentry.chunking.base import accumulate_paragraphs
from docsentry.config import ChunkingConfig
from docsentry.models import Chunk, Document, make_chunk_id

_MD = MarkdownIt("commonmark")


@dataclass
class _Open:
    """A heading currently on the stack, with whatever body has accumulated."""

    level: int
    title: str
    body: list[str] = field(default_factory=list)


class StructuralChunker:
    name = "structural"

    def __init__(self, config: ChunkingConfig) -> None:
        self.config = config

    def chunk(self, doc: Document) -> list[Chunk]:
        try:
            sections = self._sections(doc.content)
        except Exception:
            # design spec 6.3.3 降级路径: malformed Markdown falls back to
            # paragraph accumulation rather than producing nothing.
            sections = [(self._fallback_breadcrumb(doc),
                         accumulate_paragraphs(doc.content, self.config.target_size))]

        texts: list[tuple[list[str], str]] = []
        for breadcrumb, pieces in sections:
            texts.extend((breadcrumb, piece) for piece in pieces)

        chunks: list[Chunk] = []
        for ordinal, (breadcrumb, text) in enumerate(texts):
            body = text.strip()
            if not body:
                continue
            chunks.append(Chunk(
                chunk_id=make_chunk_id(doc.doc_id, ordinal, self.name),
                doc_id=doc.doc_id,
                text=text,
                breadcrumb=breadcrumb,
                source=doc.source,
                version=doc.version,
                url=doc.url,
                kind=self._classify(text),
                strategy=self.name,
                char_count=len(text),
            ))
        return chunks

    def _sections(self, content: str) -> list[tuple[list[str], list[str]]]:
        """Return ``[(breadcrumb, [piece, ...])]`` for every section with a body,
        **in document order**.

        Two things are load-bearing here:

        * **The token stream only locates the headings; the body is sliced by
          line number.** Accumulating straight from ``token.map`` duplicates
          text -- ``heading_open`` and the ``inline`` after it share one map,
          and ``paragraph_open`` / ``inline`` / ``paragraph_close`` share
          theirs, so each line is appended once per token covering it, and the
          heading line itself lands in the body.
        * **A section's body is emitted the moment a heading opens inside it**,
          not when the section is popped. The body precedes the child, so
          emitting on pop reverses the document order (C, B, A instead of
          A, B, C) and files each body under the wrong breadcrumb.

        A heading that later gains a child still owns the text before that
        child: emitting only leaf sections -- the design doc's literal wording --
        drops 19.6 points of the corpus, including the whole of
        ``build-client.md``.
        """
        lines = content.splitlines(keepends=True)
        headings = [
            (token.map[0], int(token.tag[1]), _heading_text(lines, token.map[0]))
            for token in _MD.parse(content)
            if token.type == "heading_open" and token.map
        ]

        out: list[tuple[list[str], list[str]]] = []
        stack: list[_Open] = []

        def emit(frame: _Open) -> None:
            # `stack` still contains `frame` and its ancestors, so the
            # breadcrumb is complete. Clearing the body means a parent emitted
            # ahead of its child is not emitted again when it is popped.
            pieces = self._split(frame.body)
            if pieces:
                out.append(([f.title for f in stack], pieces))
            frame.body = []

        for idx, (line_no, level, title) in enumerate(headings):
            # Close every section at this level or deeper: the new heading is
            # outside each of them, so their bodies are complete.
            while stack and stack[-1].level >= level:
                emit(stack[-1])
                stack.pop()
            # The new heading opens *inside* whatever is now on top, so that
            # section's own body ends here.
            if stack:
                emit(stack[-1])
            end = headings[idx + 1][0] if idx + 1 < len(headings) else len(lines)
            stack.append(_Open(level=level, title=title, body=lines[line_no + 1:end]))

        while stack:
            emit(stack[-1])
            stack.pop()
        return out

    def _split(self, body_lines: list[str]) -> list[str]:
        """Split one section body. Task 10 makes this atomic-unit aware."""
        text = "".join(body_lines).strip()
        if not text:
            return []
        if len(text) <= self.config.target_size:
            return [text]
        return accumulate_paragraphs(text, self.config.target_size)

    def _classify(self, text: str) -> str:
        return "prose"

    def _fallback_breadcrumb(self, doc: Document) -> list[str]:
        return [doc.title] if doc.title else []


def _heading_text(lines: list[str], line_no: int) -> str:
    match = re.match(r"^#{1,6}\s+(.*?)\s*$", lines[line_no].rstrip("\n\r"))
    return match.group(1) if match else ""
```

> ⚠️ `_split` 与 `_classify` 现在是占位实现（只做段落切分、`kind` 恒为 `prose`）。
> Task 9 和 Task 11 会把它们做真。**这是刻意的**：先让 flush-on-close 独立通过测试，
> 再加超长切分——两个改动混在一起时，失败无法归因。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_chunking_structural.py -v`
Expected: 6 passed

- [ ] **Step 5: 验证真实语料上的 18% 回来了**

```bash
uv run python -c "
from pathlib import Path
from docsentry.chunking.structural import StructuralChunker
from docsentry.config import ChunkingConfig
from docsentry.corpus.loader import load_corpus

docs = list(load_corpus(Path('data/corpus.jsonl')))
total = sum(len(d.content) for d in docs)
indexed = sum(c.char_count for d in docs for c in StructuralChunker(ChunkingConfig()).chunk(d))
print(f'corpus chars  {total:,}')
print(f'indexed chars {indexed:,}')
print(f'coverage {100*indexed/total:.1f}%')
"
```

Expected：覆盖率 **≈98%**。已在规划阶段实测过这个数：flush-on-close 得 **98.4%**，而字面的 leaf-only 读法只有 **78.8%**。

> 差的约 1.6% 是**各 section 首尾被 strip 掉的空白**，不是丢失的正文——实测 774 篇里
> **没有一篇**在第一个标题之前有正文。所以 98.4% 已经等于"正文全覆盖"。
> 覆盖率低于 90% 说明 flush 没生效，回 Step 3 查。

- [ ] **Step 6: 提交**

```bash
git add src/docsentry/chunking/structural.py tests/test_chunking_structural.py
git commit -m "feat(m2): StructuralChunker 标题栈 + flush-on-close — 救回 18% 正文"
```

---

## Task 9: `StructuralChunker` — 超长 section 切分与 `kind`

**Files:**
- Modify: `src/docsentry/chunking/structural.py`
- Test: `tests/test_chunking_structural.py`

- [ ] **Step 1: 写失败的测试**

追加到 `tests/test_chunking_structural.py`：

```python
def test_oversized_section_splits_into_children_sharing_the_breadcrumb(chunker):
    doc = make_doc("# Guide\n\n## Big\n\n" + "\n\n".join(["p" * 400] * 6) + "\n")
    chunks = chunker.chunk(doc)

    big = [c for c in chunks if c.breadcrumb == ["Guide", "Big"]]
    assert len(big) > 1, "an oversized section must be split"
    assert all(c.breadcrumb == ["Guide", "Big"] for c in big)


def test_kind_is_code_when_fences_dominate(chunker):
    doc = make_doc("# A\n\n```python\n" + "x = 1\n" * 100 + "```\n")
    assert chunker.chunk(doc)[0].kind == "code"


def test_kind_is_table_when_table_dominates(chunker):
    rows = "".join(f"| r{i} | v{i} |\n" for i in range(80))
    doc = make_doc("# A\n\n| k | v |\n|---|---|\n" + rows)
    assert chunker.chunk(doc)[0].kind == "table"


def test_kind_is_prose_for_plain_text(chunker):
    doc = make_doc("# A\n\nJust some prose about the system.\n")
    assert chunker.chunk(doc)[0].kind == "prose"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_chunking_structural.py -v -k "kind or oversized"`
Expected: FAIL — `kind` is always `"prose"`

- [ ] **Step 3: 实现**

只替换 `_classify`。**`_split` 保持 Task 8 的版本不动**——它已经做了本任务需要的段落切分，而每个子 chunk 天然继承父 section 的 breadcrumb（`_sections` 在切分前就固定了 breadcrumb，切分不改变它）。

替换 `_classify`：

```python
    def _classify(self, text: str) -> str:
        """Design spec 6.3.3 step 7: whichever structure exceeds 70% wins."""
        fenced = 0
        tabular = 0
        in_fence = False
        for line in text.splitlines(keepends=True):
            if line.lstrip().startswith(("```", "~~~")):
                in_fence = not in_fence
                fenced += len(line)
                continue
            if in_fence:
                fenced += len(line)
            elif line.lstrip().startswith("|"):
                tabular += len(line)
        total = len(text) or 1
        if fenced / total > 0.7:
            return "code"
        if tabular / total > 0.7:
            return "table"
        if (fenced + tabular) / total > 0.3:
            return "mixed"
        return "prose"
```

> `_classify` 从 `chunk()` 里被调用，接收的是最终 chunk 文本而不是整个 section —— 否则一个"前 1000 字符是散文、后面全是表格"的 section 会被判成 `code`，而那正是最该被判成 `mixed` 的情况。确认 `chunk()` 里的调用点是 `self._classify(text)`（`text` 是当前 piece）。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_chunking_structural.py -v`
Expected: 8 passed

- [ ] **Step 5: 提交**

```bash
git add src/docsentry/chunking/structural.py tests/test_chunking_structural.py
git commit -m "feat(m2): structural 超长 section 切分 + kind 判定"
```

---

## Task 10: `StructuralChunker` — 原子单元与硬上限

**Files:**
- Modify: `src/docsentry/chunking/structural.py`
- Test: `tests/test_chunking_structural.py`

- [ ] **Step 1: 写失败的测试**

```python
def test_short_code_block_is_never_cut(chunker):
    code = "```python\n" + "\n".join(f"line_{i} = {i}" for i in range(20)) + "\n```\n"
    doc = make_doc("# A\n\nIntro text.\n\n" + code)
    chunk = chunker.chunk(doc)[0]

    assert code.strip() in chunk.text
    assert chunk.split_atomic is False


def test_table_over_the_ceiling_is_split_with_repeated_header():
    """A 106,728-character table was measured on the real corpus. Left atomic it
    silently fails: the embedding truncates and the prompt cannot hold it."""
    header = "| key | value |\n| --- | --- |\n"
    rows = "".join(f"| key_{i} | value_{i} |\n" for i in range(300))
    doc = make_doc("# A\n\n" + header + rows)
    small = StructuralChunker(ChunkingConfig(target_size=1000, max_atomic_size=1500))
    chunks = small.chunk(doc)

    assert len(chunks) > 1
    assert all(c.split_atomic for c in chunks)
    for chunk in chunks:
        assert "| key | value |" in chunk.text, "each piece must carry the header"
        assert "| --- | --- |" in chunk.text


def test_code_block_over_the_ceiling_is_split_and_flagged():
    body = "\n".join(f"def f{i}():\n    return {i}" for i in range(300))
    doc = make_doc("# A\n\n```python\n" + body + "\n```\n")
    small = StructuralChunker(ChunkingConfig(target_size=1000, max_atomic_size=1500))
    chunks = small.chunk(doc)

    assert len(chunks) > 1
    assert all(c.split_atomic for c in chunks)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_chunking_structural.py -v -k "atomic or ceiling"`
Expected: FAIL — `split_atomic` is always `False`

- [ ] **Step 3: 实现**

`_sections` **不用改**——它只负责"哪段正文属于哪个 breadcrumb"，`emit()` 里调用的 `_split` 已经是新的返回类型。新增/替换以下方法：

```python
    def _split(self, body_lines: list[str]) -> list[tuple[str, bool]]:
        """Return ``[(text, split_atomic)]``.

        Code blocks and tables are atomic -- never cut mid-unit -- *unless* the
        unit exceeds ``max_atomic_size``, at which point cutting is mandatory
        rather than optional. The alternative is a chunk the embedding model
        truncates and the prompt cannot hold, which fails silently; cutting and
        flagging it fails visibly instead (design spec 6.9).

        Note the asymmetry: an atomic unit is allowed to exceed ``target_size``
        (that is the design's stated exception to the fairness constraint), but
        not ``max_atomic_size``. Those are two different ceilings and only the
        first one is negotiable.
        """
        text = "".join(body_lines).strip()
        if not text:
            return []

        blocks = _segment(text)
        single_atomic = len(blocks) == 1 and blocks[0][0] in ("code", "table")

        if len(text) <= self.config.max_atomic_size:
            if single_atomic or len(text) <= self.config.target_size:
                return [(text, False)]
            return [(piece, False)
                    for piece in accumulate_paragraphs(text, self.config.target_size)]

        pieces: list[tuple[str, bool]] = []
        buffer = ""
        for kind, block in blocks:
            if kind in ("code", "table") and len(block) > self.config.max_atomic_size:
                if buffer.strip():
                    pieces.append((buffer.strip(), False))
                    buffer = ""
                pieces.extend((piece, True) for piece in _cut_atomic(kind, block, self.config))
                continue
            if buffer and len(buffer) + len(block) + 1 > self.config.target_size:
                pieces.append((buffer.strip(), False))
                buffer = block
            else:
                buffer = f"{buffer}\n{block}" if buffer else block
        if buffer.strip():
            pieces.append((buffer.strip(), False))
        return pieces
```

以及模块级辅助函数：

```python
FENCE_RE = re.compile(r"^[ \t]*(```|~~~)")


def _segment(text: str) -> list[tuple[str, str]]:
    """Split ``text`` into ``(kind, block)`` where kind is code | table | prose."""
    blocks: list[tuple[str, str]] = []
    current: list[str] = []
    in_fence = False
    for line in text.splitlines(keepends=True):
        if FENCE_RE.match(line):
            in_fence = not in_fence
            current.append(line)
            continue
        if not in_fence and not line.strip():
            if current:
                blocks.append((_kind_of(current), "".join(current)))
                current = []
            continue
        current.append(line)
    if current:
        blocks.append((_kind_of(current), "".join(current)))
    return [(k, b) for k, b in blocks if b.strip()]


def _kind_of(lines: list[str]) -> str:
    joined = "".join(lines).lstrip()
    if joined.startswith(("```", "~~~")):
        return "code"
    if joined.startswith("|"):
        return "table"
    return "prose"


def _cut_atomic(kind: str, block: str, config: ChunkingConfig) -> list[str]:
    """Cut an oversized atomic unit. Tables repeat their header in every piece."""
    if kind == "table":
        lines = block.splitlines(keepends=True)
        header = lines[:2]
        rows = lines[2:]
        pieces: list[str] = []
        buffer: list[str] = []
        size = len("".join(header))
        for row in rows:
            if buffer and size + len(row) > config.target_size:
                pieces.append("".join(header + buffer))
                buffer, size = [], len("".join(header))
            buffer.append(row)
            size += len(row)
        if buffer:
            pieces.append("".join(header + buffer))
        return pieces

    # Code: prefer blank lines and top-level def/class/function starts. A
    # heuristic -- it can cut inside a nested block, so the chunk is flagged.
    lines = block.splitlines(keepends=True)
    pieces: list[str] = []
    buffer: list[str] = []
    size = 0
    for line in lines:
        starts_top_level = bool(re.match(r"^(def |class |function |async def )", line))
        if buffer and (size + len(line) > config.max_code_size or starts_top_level):
            pieces.append("".join(buffer))
            buffer, size = [], 0
        buffer.append(line)
        size += len(line)
    if buffer:
        pieces.append("".join(buffer))
    return pieces
```

在 `chunk()` 里，把收集正文那段改成带 `split_atomic` 的三元组：

```python
        texts: list[tuple[list[str], str, bool]] = []
        for breadcrumb, pieces in sections:
            texts.extend((breadcrumb, piece, atomic) for piece, atomic in pieces)
```

并在构造 `Chunk(...)` 时加一行 `split_atomic=atomic`。

> `_sections` 与 `emit()` **不需要改**：`emit` 调用 `self._split(frame.body)`，而 `_split` 的返回类型已经变了，`out` 里存的自然就是 `[(str, bool)]`。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_chunking_structural.py -v`
Expected: 11 passed

- [ ] **Step 5: 在真实语料上确认没有超级 chunk**

```bash
uv run python -c "
from pathlib import Path
from docsentry.chunking.structural import StructuralChunker
from docsentry.config import ChunkingConfig
from docsentry.corpus.loader import load_corpus

chunker = StructuralChunker(ChunkingConfig())
sizes = [c.char_count for d in load_corpus(Path('data/corpus.jsonl')) for c in chunker.chunk(d)]
sizes.sort()
n = len(sizes)
print(f'chunks {n:,}  mean {sum(sizes)/n:.0f}  median {sizes[n//2]}  max {sizes[-1]:,}')
print(f'over 4000: {sum(1 for s in sizes if s > 4000):,}')
"
```

Expected：**`max` 应当远小于 106,728**（理想 ≤ 8,000）。若 `max` 仍有几十万，说明有原子单元没走进 `_cut_atomic`——查 `_segment` 是否把该块正确归类为 `code`/`table`。

- [ ] **Step 6: 提交**

```bash
git add src/docsentry/chunking/structural.py tests/test_chunking_structural.py
git commit -m "feat(m2): 原子单元硬上限 — 106KB 单 chunk 不再存在"
```

---

## Task 11: 噪声丢弃与丢弃计数

**Files:**
- Modify: `src/docsentry/chunking/structural.py`
- Modify: `src/docsentry/chunking/base.py`
- Test: `tests/test_chunking_structural.py`, `tests/test_chunking_base.py`

设计文档 §6.3.3 step 8：正文剥掉标签后为空则丢弃，**丢弃数必须进报告**。

- [ ] **Step 1: 写失败的测试**

追加到 `tests/test_chunking_structural.py`：

```python
def test_html_only_section_is_dropped_and_counted(chunker):
    """Real example: `/specification/.../authorization.md` carries a section
    whose entire body is `<div id="enable-section-numbers" />`."""
    doc = make_doc(
        "# Authorization\n\n"
        "## Real\n\nActual content here.\n\n"
        "## Stub\n\n<div id=\"enable-section-numbers\" />\n"
    )
    chunks = chunker.chunk(doc)

    assert all("enable-section-numbers" not in c.text for c in chunks)
    assert chunker.last_dropped == 1
```

追加到 `tests/test_chunking_base.py`：

```python
def test_is_noise_strips_tags_before_judging():
    from docsentry.chunking.base import is_noise

    assert is_noise('<div id="x" />')
    assert is_noise("   \n\n  ")
    assert not is_noise("Real content.")
    assert not is_noise("# heading only")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_chunking_structural.py tests/test_chunking_base.py -v -k "noise or dropped"`
Expected: FAIL — `is_noise` missing

- [ ] **Step 3: 实现**

追加到 `base.py`：

```python
_TAG_RE = re.compile(r"<[^>]+>")


def is_noise(text: str) -> bool:
    """True when a chunk carries no retrievable content at all.

    The bar is deliberately narrow: after stripping HTML tags, nothing is left.
    Sampled from the real corpus, sub-200-character sections are mostly
    *useful* -- precise API fragments like "Delete items from vector store"
    (179 characters) are exactly what a developer asks about, so they are kept
    and not merged (design spec 6.3.3 step 8). The genuinely empty ones are the
    handful whose whole body is a layout artefact such as
    ``<div id="enable-section-numbers" />``.

    A heuristic that also dropped "short" text would have to pick a threshold,
    and every threshold in the 100-500 range throws away chunks the sample shows
    to be good. "Empty after stripping tags" needs no threshold.
    """
    return not _TAG_RE.sub("", text).strip()
```

在 `structural.py` 的 `__init__` 里加 `self.last_dropped = 0`，并在 `chunk()` 开头重置它：

```python
    def __init__(self, config: ChunkingConfig) -> None:
        self.config = config
        # Dropped-chunk count for this document. Reported, never silent --
        # a chunk that disappears without a number is indistinguishable from
        # one that was never produced.
        self.last_dropped = 0
```

把 `chunk()` 里的 `if not body: continue` 改成：

```python
            if not body or is_noise(body):
                self.last_dropped += 1
                continue
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_chunking_base.py tests/test_chunking_structural.py -v`
Expected: 全绿

- [ ] **Step 5: 提交**

```bash
git add src/docsentry/chunking/ tests/test_chunking_base.py tests/test_chunking_structural.py
git commit -m "feat(m2): 纯噪声 chunk 丢弃并计数，不静默"
```

---

## Task 12: `chunking/registry.py` — 策略名到实例

**Files:**
- Create: `src/docsentry/chunking/registry.py`
- Test: `tests/test_chunking_registry.py`

- [ ] **Step 1: 写失败的测试**

```python
# tests/test_chunking_registry.py
import pytest

from docsentry.chunking.registry import STRATEGIES, build_chunker
from docsentry.config import ChunkingConfig


def test_all_three_strategies_are_reachable():
    assert set(STRATEGIES) == {"fixed", "semantic", "structural"}


@pytest.mark.parametrize("name", ["fixed", "semantic", "structural"])
def test_build_returns_a_chunker_with_matching_name(name):
    chunker = build_chunker(name, ChunkingConfig())
    assert chunker.name == name


def test_unknown_strategy_fails_loudly():
    with pytest.raises(KeyError):
        build_chunker("nope", ChunkingConfig())
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_chunking_registry.py -v`
Expected: FAIL — module missing

- [ ] **Step 3: 写实现**

```python
# src/docsentry/chunking/registry.py
"""Strategy name -> chunker. One place so the CLI, the indexer and the fairness
check cannot drift apart on what the strategies are called."""

from __future__ import annotations

from docsentry.chunking.base import Chunker
from docsentry.chunking.fixed import FixedSizeChunker
from docsentry.chunking.semantic import SemanticChunker
from docsentry.chunking.structural import StructuralChunker
from docsentry.config import ChunkingConfig

STRATEGIES: dict[str, type] = {
    "fixed": FixedSizeChunker,
    "semantic": SemanticChunker,
    "structural": StructuralChunker,
}


def build_chunker(name: str, config: ChunkingConfig) -> Chunker:
    return STRATEGIES[name](config)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_chunking_registry.py -v`
Expected: 5 passed

- [ ] **Step 5: 提交**

```bash
git add src/docsentry/chunking/registry.py tests/test_chunking_registry.py
git commit -m "feat(m2): 策略注册表"
```

---

## Task 13: `scripts/check_fairness.py` — 会跑的公平性产物

**Files:**
- Create: `scripts/check_fairness.py`
- Test: `tests/test_check_fairness_cli.py`

设计文档 §6.3 的公平性约束如果只写在测试里，就会变成"每个策略各自断言自己的尺寸，没人断言三者之间的关系"——M1 的教训是"各自绿 ≠ 组合起来对"。所以这是一个真的会跑的脚本。

- [ ] **Step 1: 写失败的测试**

```python
# tests/test_check_fairness_cli.py
from typer.testing import CliRunner

from scripts.check_fairness import app

runner = CliRunner()


def test_report_shape(tmp_path):
    """A tiny corpus is enough to exercise the plumbing."""
    import json

    from datetime import datetime, timezone

    corpus = tmp_path / "corpus.jsonl"
    doc = {
        "doc_id": "d1", "source": "s", "kind": "llms_txt", "version": "v",
        "url": "u", "title": "T", "path": "p",
        "content": "# A\n\n" + "x" * 3000 + "\n\n## B\n\n" + "y" * 3000 + "\n",
        "content_hash": "h", "origin_format": "md",
        "fetched_at": datetime(2026, 9, 24, tzinfo=timezone.utc).isoformat(),
        "indexed_at": None,
    }
    corpus.write_text(json.dumps(doc) + "\n", encoding="utf-8")

    result = runner.invoke(app, ["--corpus", str(corpus)])
    assert result.exit_code == 0
    assert "fixed" in result.stdout
    assert "semantic" in result.stdout
    assert "structural" in result.stdout
    assert "total_chars" in result.stdout
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_check_fairness_cli.py -v`
Expected: FAIL — module missing

- [ ] **Step 3: 写实现**

```python
# scripts/check_fairness.py
"""Fairness report for the three chunkers -- design spec 6.3.

The fairness constraint is only meaningful if someone actually checks it. A test
asserting each strategy's own size says nothing about the relationship *between*
strategies, which is exactly the seam M1 shipped a bug through -- three tasks
each green, and the combination aborted the run.

Publishes mean, median, chunk count and **total indexed characters**. That last
one is the number that exposes the overlap difference (fixed 1.25x the corpus,
the structure-aware strategies 1.00x); mean size alone hides it.
"""

from __future__ import annotations

from pathlib import Path

import typer

from docsentry.chunking.base import SizeStats, accumulate_paragraphs
from docsentry.chunking.registry import STRATEGIES, build_chunker
from docsentry.config import ChunkingConfig, load_chunking_config
from docsentry.corpus.loader import load_corpus

app = typer.Typer(add_completion=False)

MAX_RATIO = 1.30


@app.command()
def main(
    corpus: Path = typer.Option(Path("data/corpus.jsonl"), "--corpus"),
    chunking_config: Path = typer.Option(Path("configs/chunking.yaml"), "--config"),
    limit: int = typer.Option(0, help="0 = all documents"),
) -> None:
    config = load_chunking_config(chunking_config)
    documents = list(load_corpus(corpus))
    if limit:
        documents = documents[:limit]

    corpus_chars = sum(len(d.content) for d in documents)
    results: dict[str, SizeStats] = {}

    for name in STRATEGIES:
        chunker = build_chunker(name, config)
        sizes: list[int] = []
        for doc in documents:
            sizes.extend(c.char_count for c in chunker.chunk(doc))
        results[name] = SizeStats.from_sizes(sizes)

    typer.echo(f"docs {len(documents):,}   corpus_chars {corpus_chars:,}\n")
    header = f"{'strategy':<12}{'chunks':>9}{'mean':>9}{'median':>9}{'total_chars':>14}{'coverage':>10}"
    typer.echo(header)
    typer.echo("-" * len(header))
    for name, stats in results.items():
        coverage = stats.total_chars / corpus_chars if corpus_chars else 0
        typer.echo(f"{name:<12}{stats.count:>9,}{stats.mean:>9.0f}"
                   f"{stats.median:>9.0f}{stats.total_chars:>14,}{coverage:>9.2f}x")

    means = [s.mean for s in results.values() if s.mean]
    if means:
        ratio = max(means) / min(means)
        typer.echo(f"\nmean ratio (worst pair) {ratio:.2f}x   threshold {MAX_RATIO:.2f}x")
        typer.echo("FAIR" if ratio <= MAX_RATIO else "UNFAIR -- sizes differ too much to attribute a gap to *where* the split happens")
        if ratio > MAX_RATIO:
            raise typer.Exit(code=1)


if __name__ == "__main__":
    app()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_check_fairness_cli.py -v`
Expected: 1 passed

- [ ] **Step 5: 在真实语料上跑一次**

```bash
uv run scripts/check_fairness.py
```

Expected：三行数据 + `mean ratio ... FAIR`。**把这张表抄进设计文档与 README**（替换掉那些 MDX 修复前的旧数字）。

- [ ] **Step 6: 提交**

```bash
git add scripts/check_fairness.py tests/test_check_fairness_cli.py
git commit -m "feat(m2): 公平性检查产物 — 均值/中位数/总数/总字符数"
```

---

## Task 14: 真实语料验收测试与文档回填

**Files:**
- Create: `tests/test_m2_acceptance.py`
- Modify: `pyproject.toml`（新增 `corpus` 标记）
- Modify: `docs/superpowers/specs/2026-09-24-m2-chunking-design.md`
- Modify: `README.md`

- [ ] **Step 1: 加标记**

在 `pyproject.toml` 的 `markers` 里追加：

```toml
    "corpus: needs data/corpus.jsonl; run with `uv run pytest -m corpus`",
```

并把 `addopts` 改成同时排除 `network` 与 `corpus`：

```toml
addopts = "-q -m 'not network and not corpus'"
```

- [ ] **Step 2: 写验收测试**

```python
# tests/test_m2_acceptance.py
"""M2 acceptance against the real corpus.

Marked `corpus` and excluded by default. Assertions use *lower bounds* and
*ratios*, never exact totals: the corpus moves underneath us (LangChain's index
restructured 2026-09-20 -> 09-22, 369 -> 539 documents).
"""

from pathlib import Path

import pytest

from docsentry.chunking.registry import STRATEGIES, build_chunker
from docsentry.config import ChunkingConfig, load_chunking_config
from docsentry.corpus.loader import load_corpus

pytestmark = pytest.mark.corpus

CORPUS = Path("data/corpus.jsonl")


@pytest.fixture(scope="module")
def documents():
    if not CORPUS.exists():
        pytest.skip("no corpus; run scripts/fetch_corpus.py first")
    return list(load_corpus(CORPUS))


@pytest.fixture(scope="module")
def config():
    return load_chunking_config(Path("configs/chunking.yaml"))


def test_every_document_still_yields_chunks(documents, config):
    """No document may vanish silently -- the corpus layer's whole premise."""
    empty = []
    for name in STRATEGIES:
        chunker = build_chunker(name, config)
        for doc in documents:
            if not chunker.chunk(doc):
                empty.append((name, doc.url))
    assert empty == []


def test_structural_covers_the_corpus(documents, config):
    """The literal "leaf sections" reading covered 78.8%; flush-on-close
    measured 98.4% at planning time. The residual ~1.6% is whitespace stripped
    from section edges -- no document has body text before its first heading."""
    chunker = build_chunker("structural", config)
    total = sum(len(d.content) for d in documents)
    indexed = sum(c.char_count for d in documents for c in chunker.chunk(d))

    assert indexed / total > 0.95


def test_no_chunk_is_pathologically_large(documents, config):
    """The atomic-unit rule without a ceiling produced a 106,728-character
    chunk on the real corpus."""
    chunker = build_chunker("structural", config)
    biggest = max((c.char_count for d in documents for c in chunker.chunk(d)), default=0)

    assert biggest < 12_000, f"largest chunk {biggest:,} -- capture in the report if intentional"


def test_mdx_normalisation_reached_the_corpus(documents):
    """`build-client.md` parses as 2 headings before MDX normalisation and 127
    after. If the corpus was never rebuilt, this fails and the whole M2
    comparison is measuring garbage."""
    target = [d for d in documents if d.url.endswith("develop/build-client.md")]
    if not target:
        pytest.skip("build-client.md not in this corpus")

    from markdown_it import MarkdownIt

    tokens = MarkdownIt("commonmark").parse(target[0].content)
    headings = [t for t in tokens if t.type == "heading_open"]
    assert len(headings) > 50, (
        f"only {len(headings)} headings -- the corpus predates MDX normalisation; "
        "run scripts/fetch_corpus.py --update"
    )


def test_strategies_are_within_the_fairness_band(documents, config):
    means = {}
    for name in STRATEGIES:
        chunker = build_chunker(name, config)
        sizes = [c.char_count for d in documents for c in chunker.chunk(d)]
        means[name] = sum(sizes) / len(sizes)

    ratio = max(means.values()) / min(means.values())
    assert ratio <= 1.30, f"mean sizes drifted apart: {means}"
```

- [ ] **Step 3: 跑验收**

```bash
uv run pytest -m corpus -v
```

Expected: 5 passed

- [ ] **Step 4: 把真实数字回填文档**

用 `uv run scripts/check_fairness.py` 的输出替换设计文档与 README 里的旧数字。**要更新的位置**：

| 文件 | 位置 | 改什么 |
|---|---|---|
| `2026-09-24-m2-chunking-design.md` | 开头引用块 | 去掉"数字是 MDX 修复前测的"那句，换成实测表 |
| 同上 | §2 决策 3 | 均值比值 1.08× → 新值 |
| `README.md` | Progress | 勾上 Chunking strategies，写清三策略与公平性结论 |
| `README.md` | Experiments 表 | 补第 1 项的行 |

- [ ] **Step 5: 跑全量测试**

```bash
uv run pytest -q
uv run pytest -m corpus -q
uv run pytest -m network -q      # 可选，约 5 分钟
```

Expected：三条都绿。默认那条应当比 M1 的 192 条多出约 30 条。

- [ ] **Step 6: 提交**

```bash
git add tests/test_m2_acceptance.py pyproject.toml README.md docs/
git commit -m "test(m2): 真实语料验收 + 文档回填实测数字"
```

---

## 自查清单（实现者收尾时逐条确认）

- [ ] `uv run pytest -q` 全绿，且默认不跑 `network` / `corpus`
- [ ] `uv run pytest -m corpus -v` 全绿
- [ ] `uv run scripts/check_fairness.py` 输出 `FAIR`
- [ ] `uv run scripts/health.py` 仍是 774 篇
- [ ] 设计文档与 README 里**不再有** MDX 修复前的旧尺寸数字
- [ ] `pyproject.toml` 显式声明了 `markdown-it-py`，且 `uv.lock` 一并提交
- [ ] 没有任何 chunk 超过 12,000 字符（超了就说明原子单元的上限没生效）
- [ ] `structural` 的语料覆盖率 > 95%（低于就说明 flush 没生效）

## 明确不做（设计文档 §8.4 与本次讨论已排除）

- **不合并小碎片**：抽样 18 条后确认多数是精确 API 片段，合并会破坏 breadcrumb 精确性
- **不给 `semantic` / `structural` 加 overlap**：overlap 是硬切对边界无知的补偿，披露而非消除
- **不做 `local_dir` 源的 MDX 归一化**：私有语料不会是 Mintlify 格式，归一化对它是空操作
- **不做 PDF/Office 转换器**：选做项，见 CLAUDE.md 砍功能顺序表
